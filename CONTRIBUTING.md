# Contributing

## Continuous integration

Every pull request to `master` builds the Sample App via GitHub Actions. See [`.github/workflows/ci.yml`](.github/workflows/ci.yml) for the workflow definition.
